function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(237, 34, 93);
  fill(0);

  if (mouseIsPressed) {
    background(50, 50, 50);
  } else {
    (mouseX,165,185,mouseY,165,185);
    
  }
rect(165, 165, 100, 100);
  print(mouseIsPressed);
}
